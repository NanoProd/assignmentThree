package DataTypes;

public class DefaultComparator<E> implements Comparable<E> {
    public int compareTo(E a, E b) throws ClassCastException{
        return ((Comparable<E>) a).compareTo(b);
    }
}
